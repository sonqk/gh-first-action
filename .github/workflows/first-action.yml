name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greating
        run: echo "hello world"
      - name: Print Goodbye
        run: echo "done - bye"
